# 模型推理云平台 — 产品方案文档

> **版本**: v1.0  
> **日期**: 2026-04-03  
> **文档类型**: 产品功能设计 + 系统架构 + 技术选型

---

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 产品定位与目标用户](#2-产品定位与目标用户)
- [3. 部署模式](#3-部署模式)
  - [3.3 国产化算力适配](#33-国产化算力适配)
- [4. 功能设计](#4-功能设计)
  - [4.1 推理引擎](#41-推理引擎)
    - [4.1.2 API 规范 (Responses API + Chat Completions)](#412-api-规范)
  - [4.2 Playground 交互式试玩](#42-playground-交互式试玩)
  - [4.3 专属端点 (Dedicated Endpoints)](#43-专属端点-dedicated-endpoints)
  - [4.4 模型微调 (Fine-tuning)](#44-模型微调-fine-tuning)
  - [4.5 数据实验室 (Data Lab)](#45-数据实验室-data-lab)
  - [4.6 批量推理 (Batch API)](#46-批量推理-batch-api)
  - [4.7 可观测性 (Observability)](#47-可观测性-observability)
  - [4.8 团队管理与权限控制](#48-团队管理与权限控制)
  - [4.9 计费系统](#49-计费系统)
  - [4.10 第三方集成](#410-第三方集成)
  - [4.11 迁移指南 (Switch Guide)](#411-迁移指南-switch-guide)
  - [4.12 Cookbook 开发者示例库](#412-cookbook-开发者示例库)
  - [4.13 CLI 命令行工具](#413-cli-命令行工具)
- [5. 系统架构](#5-系统架构)
  - [5.1 总体架构分层](#51-总体架构分层)
  - [5.2 推理请求数据流](#52-推理请求数据流)
  - [5.3 批量推理数据流](#53-批量推理数据流)
  - [5.4 微调训练数据流](#54-微调训练数据流)
  - [5.5 自动伸缩机制](#55-自动伸缩机制)
- [6. 技术选型](#6-技术选型)
  - [6.1 推理引擎层](#61-推理引擎层)
  - [6.2 后端服务层](#62-后端服务层)
  - [6.3 前端技术](#63-前端技术)
  - [6.4 存储方案](#64-存储方案)
  - [6.5 基础设施与编排](#65-基础设施与编排)
  - [6.6 可观测性工具链](#66-可观测性工具链)
- [7. 安全与合规](#7-安全与合规)
- [8. 非功能性需求](#8-非功能性需求)
- [9. 实施路线图](#9-实施路线图)
- [10. 风险与应对](#10-风险与应对)

---

## 1. 产品概述

本方案旨在设计并构建一个 **厂商中立的一站式模型推理云平台**，为企业和开发者提供从模型探索、推理部署、数据管理、微调训练到生产运维的全链路闭环能力。平台以 **OpenAI 兼容 API** 为核心接口规范，最大化降低用户迁移成本；以 **Lance/LanceDB 为 AI 原生数据底座**，实现向量 + 多模态数据的高性能存储与混合查询。支持主流开源大语言模型及多模态模型的高性能推理与定制化训练，同时深度适配国产 GPU 算力，满足信创合规需求。

**核心价值主张：**

- **开箱即用** — 零运维即可调用主流开源模型，按 Token 弹性计费
- **灵活部署** — 从 Serverless 共享推理到独占 GPU 专属端点，匹配不同规模需求
- **全链路闭环** — 推理 → Data Lab 数据采集 → SQL 筛选 → 微调训练 → 模型部署，独有的闭环模型迭代工作流，消除跨工具数据搬运
- **AI 原生数据底座** — 基于 Lance 列式格式和 LanceDB 向量数据库构建数据层，随机访问比 Parquet 快 100x，原生支持向量 + 多模态混合存储与查询，Git-style 数据集版本管理，<1ms 语义缓存命中
- **厂商中立** — 不绑定模型厂商（同时支持 DeepSeek、Qwen、GLM、Kimi、Llama 等 7+ 厂商）、不绑定云厂商，集成 30+ 第三方框架与工具
- **企业就绪** — RBAC 权限、SSO 登录、零数据留存、合规认证（SOC 2 / GDPR / HIPAA），满足企业级安全合规要求
- **国产化适配** — 支持华为昇腾、海光、寒武纪等国产 GPU，通过 HAMi 异构中间件统一抽象，深度适配国产大模型，满足信创与国产化合规需求

---

## 2. 产品定位与目标用户

### 2.1 竞品对标

| 竞品 | 定位 | 我们的差异化 |
|------|------|------------|
| **海外平台** | | |
| Together AI | Serverless 推理+微调 | 独有 Data Lab 闭环 + AI 原生数据底座（Lance/LanceDB）+ 国产 GPU 适配 |
| Fireworks AI | 高性能推理 API | Data Lab 微调闭环 + 语义缓存 + Responses API 先发优势 |
| Replicate | 模型托管平台 | 全栈推理优化（PD 分离/投机解码）+ 数据集版本管理 + 企业级安全 |
| **国产平台** | | |
| 硅基流动 (SiliconFlow) | 低成本推理云服务，自研 SiliconLLM 引擎 | 完整企业功能 + 专属端点 + AI 原生数据层 + 国产 GPU 全系深度适配 |
| 火山引擎 (豆包/方舟) | 字节跳动 AI 推理套件，依托内部实践 | 厂商中立开放生态 + 国产化适配 + Lance 数据版本管理，不绑定单一云厂商 |
| 阿里云百炼 | 全栈大模型服务平台，知识库+Agent | 厂商中立 + 独立 Data Lab（Lance 存储 + 向量混合查询）+ 多引擎推理 |
| 智谱 BigModel | GLM 系列模型开放平台 | 多模型厂商中立 + AI 原生数据底座 + 完整全链路能力，不限自有模型生态 |
| 百度千帆 | 文心大模型+企业智能体平台 | 开源模型优先 + 语义缓存 + 数据闭环，避免厂商锁定 |
| 腾讯混元 | 混元大模型 API 服务 | 推理优化纵深 + Data Lab + CLI 工具链 + AI 原生存储 |

### 2.2 目标用户

| 用户类型 | 典型场景 | 关注点 |
|---------|---------|--------|
| **个人开发者** | 原型验证、个人项目 | 易用性、免费额度、模型丰富度 |
| **初创团队** | AI 产品 MVP、SaaS 应用 | 成本控制、API 兼容性、快速集成 |
| **中型企业** | 生产级 AI 应用 | 性能稳定性、自动伸缩、可观测性 |
| **大型企业** | 核心业务系统、合规场景 | 安全合规、专属端点、SLA 保障 |
| **AI 研究团队** | 模型评测、微调实验 | 模型多样性、微调能力、批量推理 |

---

## 3. 部署模式

平台提供两种部署模式，覆盖从轻量实验到企业生产的完整场景：

### 3.1 Serverless 共享推理

- **描述**：多租户共享 GPU 集群，平台统一管理模型实例和资源调度
- **计费**：按 Token 用量计费（输入/输出分别定价）
- **适用**：开发测试、中小规模生产、对延迟要求非极端严格的场景
- **特点**：零运维、即开即用、自动弹性伸缩

### 3.2 专属端点 (Dedicated Endpoints)

- **描述**：为用户分配独立的 GPU 实例，运行指定模型的隔离推理服务
- **计费**：按 GPU·时 计费（副本数 × GPU 数量 × 运行时间）
- **适用**：对延迟/吞吐有严格要求的生产场景
- **特点**：资源隔离、可配置副本与 GPU 类型、自动伸缩 (min/max replicas)

### 3.3 国产化算力适配

平台支持 NVIDIA 与国产 GPU 异构混合部署，满足信创和国产化合规要求：

| GPU 类型 | 厂商 | 适用场景 | 推理引擎适配 |
|---------|------|---------|------------|
| **H100 / H200 / B200** | NVIDIA | 高性能推理与训练 (主力) | vLLM / SGLang |
| **L40S** | NVIDIA | 性价比推理 | vLLM / SGLang |
| **昇腾 910B / 950** | 华为 | 国产化合规推理，Atlas 350 单卡算力 2.87× H20 | vLLM-Ascend / LMDeploy |
| **海光 K100** | 海光信息 | 信创环境推理 | LMDeploy / 定制适配 |
| **思元 590** | 寒武纪 | 国产化推理，FP8+INT4 混合量化 | LMDeploy |

**异构调度策略**：
- 通过 HAMi (异构 AI 计算虚拟化中间件) 实现国产 GPU 统一资源抽象
- 支持基于成本/性能/合规要求的智能路由，自动选择最优 GPU 集群
- 国产 GPU 集群与 NVIDIA 集群独立部署，API 层透明切换

---

## 4. 功能设计

平台共划分为 **13 个功能模块**，覆盖推理服务、模型管理、数据闭环、运维监控、开发者工具、安全合规全链路：

| 功能模块 | 一级功能 | 功能简介 |
|---------|---------|---------|
| **4.1 推理引擎** | 模型推理服务 | 平台核心引擎，支持 Text/Vision/Embedding/Rerank/Image/Video/Speech 七大模型类型；提供 Responses API + Chat Completions 双格式接口；集成 FlashAttention-4、KV Cache 压缩、投机解码、PD 分离等前沿优化技术；支持 Serverless 共享推理与专属端点两种部署模式 |
| **4.2 Playground** | 交互式试玩 | Web 端模型交互界面，支持参数实时调节、模型对比 (Compare)、Function Calling 测试、结构化输出 (JSON Schema) 测试、Prompt Preset 管理与代码导出，帮助开发者快速验证模型效果 |
| **4.3 专属端点** | Dedicated Endpoints | 为客户提供独占 GPU 实例的专属推理服务，支持 Template 模板管理、弹性伸缩 (min/max replicas)、缩至零、多 GPU 类型选择（含国产 GPU），按 GPU·小时计费 |
| **4.4 模型微调** | Fine-tuning | 支持 LoRA 和全参数微调两种训练方式，提供训练任务全生命周期管理（创建/监控/取消）、数据集格式验证、Checkpoint 管理、LoRA 适配器挂载与合并 |
| **4.5 数据实验室** | Data Lab | 统一的数据工作空间，汇聚推理日志与训练数据集；支持 Chat Completions 日志导入、Datasets API（CRUD/分片上传/SQL 查询/导出）、数据标注、PII 脱敏与合规处理 |
| **4.6 批量推理** | Batch API | 异步批量推理接口，适用于大规模离线评测、数据标注等场景；享受折扣定价，不占用实时推理速率配额；支持任务拆分、优先级调度、结果文件下载 |
| **4.7 可观测性** | Observability | 多维度实时监控体系，覆盖延迟 (TTFT/TPS/E2E)、吞吐 (RPM/TPM)、错误率、GPU 利用率、队列深度、自动伸缩等指标；支持 Prometheus 导出、Grafana 仪表盘和 Webhook 告警 |
| **4.8 团队管理** | 权限控制 | 组织 → 项目两级层级管理，基于角色的访问控制 (RBAC)；支持 SSO (SAML/OIDC)、MFA 多因素认证；API Key 粒度权限管控（项目隔离/过期/IP 白名单） |
| **4.9 计费系统** | Billing | 按 Token 计量 + GPU·小时双计费模式；支持支付宝、微信支付、银行转账、Promo Code 充值、新用户试用额度；提供实时余额监控、自动充值阈值、用量趋势分析和成本预警 |
| **4.10 第三方集成** | 生态集成 | 对接主流 AI 框架 (LangChain/LlamaIndex/CrewAI)、Agent 框架 (Agno/AutoGen/Camel)、Coding Assistants (Cursor/Continue/Cline)、API Routers (LiteLLM/OpenRouter) 及国产生态 (魔搭/DashScope/钉钉/飞书) |
| **4.11 迁移指南** | Switch Guide | 面向从 OpenAI 等平台迁移的用户，提供零成本迁移方案（仅需改 base_url + api_key）、完整的 API 兼容性矩阵、代码示例和逐步迁移文档 |
| **4.12 Cookbook** | 开发者示例库 | 官方维护的开源代码仓库 (GitHub)，涵盖 API 调用、Agent 框架集成、RAG 管道、微调蒸馏、多模态等场景的完整示例，作为新用户 Onboarding 教程 |
| **4.13 CLI 工具** | 命令行客户端 | Go 单二进制跨平台 CLI，覆盖推理/微调/批量/端点/数据集/用量全流程操作；支持交互式 REPL 对话与脚本化 CI/CD 双模式；Homebrew/npm/pip/Docker 多渠道分发 |

---

### 4.1 推理引擎

推理引擎是平台的核心，负责接收用户请求并返回模型生成结果。

#### 4.1.1 支持的模型类型

| 类型 | 说明 | 典型模型 |
|------|------|---------|
| **Text-to-Text** | 对话补全、代码生成、翻译、摘要 | DeepSeek-V4/R1, Qwen3.5, Llama 4, Mistral, GLM-5, Kimi-K2.5, 豆包 2.0 |
| **Vision (多模态)** | 图文理解、文档解析、视觉问答 | Qwen2.5-VL, InternVL3, Llama 4, GLM-4V, CogVLM |
| **Embedding** | 文本向量化、语义检索 | BGE-M3, Jina-Embeddings-v3, GTE (通义) |
| **Rerank** | 文档重排序、检索增强 | BGE-Reranker, Jina-Reranker, Qwen3-Reranker |
| **Text-to-Image** | 图像生成 | Flux.1, SDXL, Kolors, CogView-3 |
| **Text-to-Video** | 视频生成 | CogVideoX, Seedance, HunyuanVideo |
| **Speech** | 语音合成与识别 | CosyVoice (通义), SenseVoice, Whisper |

**国产开源模型重点支持矩阵：**

| 厂商 | 模型系列 | 最新版本 | 特点 |
|------|---------|---------|------|
| DeepSeek (深度求索) | DeepSeek-V4 / R1 | V4 (MoE, 100万上下文) | 极致性价比，推理能力业界领先 |
| 阿里云 | Qwen 3.5 / Qwen4 | Qwen3.5 (397B) | Dense + MoE 双架构，全尺寸覆盖 |
| 智谱 AI | GLM-5 | GLM-5 | 深度适配 7 种国产芯片，中文 Benchmark 领先 |
| 月之暗面 | Kimi K2.5 | K2.5 (1T, 256K) | 超大参数规模，长上下文能力突出 |
| 书生 (上海 AI Lab) | InternLM / InternVL | InternVL3 | 多模态理解能力强，开源社区活跃 |
| 字节跳动 | 豆包 (Doubao) | 2.0 Pro/Lite/Mini | Pro/Lite/Mini 多版本，中国消费市场领先 |
| 百川智能 | Baichuan | Baichuan4 | 中文对话与搜索增强能力 |

#### 4.1.2 API 规范

完全兼容 OpenAI API 规范，同时支持 **Chat Completions API** 和 **Responses API** 两种格式，降低迁移成本：

**推理端点**

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/responses` | POST | **Responses API** — 新一代统一推理接口，支持 Items 模型、服务端状态管理、内置工具调用 |
| `/v1/responses/{id}` | GET | 查询已存储的 Response 详情 |
| `/v1/responses/{id}/input_items` | GET | 获取 Response 的输入 Items 列表 |
| `/v1/chat/completions` | POST | **Chat Completions API** — 经典对话补全接口（持续支持），支持 SSE 流式输出 |
| `/v1/completions` | POST | 文本补全（FIM 代码补全） |
| `/v1/embeddings` | POST | 文本向量嵌入 |
| `/v1/images/generations` | POST | 图像生成 |
| `/v1/rerank` | POST | 文档重排序（Rerank） |

**管理端点**

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/models` | GET | 模型列表与详情查询 |
| `/v1/models` (Custom) | POST/GET/DELETE | 自定义模型创建、查询与删除 |
| `/v1/batches` | POST/GET | 批量推理任务管理 |
| `/v1/files` | POST/GET/DELETE | 文件上传与管理 |
| `/v1/fine_tuning/jobs` | POST/GET | 微调任务管理 |
| `/v1/datasets` | POST/GET/DELETE | 数据集上传、查询、导出、SQL 过滤 |

#### 4.1.2.1 Responses API 详细设计

Responses API 是对 Chat Completions 的演进，推荐所有新项目使用。核心差异：

| 维度 | Chat Completions | Responses API |
|------|------------------|---------------|
| 数据单元 | Messages（消息数组） | Items（类型化联合体：message / function_call / function_call_output） |
| 系统指令 | `messages[].role = "system"` | 顶层 `instructions` 参数 |
| 输入格式 | `messages` 数组（必须完整对话历史） | `input`（字符串 / Items 数组 / 引用 previous_response_id） |
| 输出格式 | `choices[].message` | `output`（Items 数组）+ `output_text` 快捷属性 |
| 状态管理 | 客户端手动维护完整对话上下文 | 服务端存储 (`store: true`)，通过 `previous_response_id` 链式引用 |
| 结构化输出 | `response_format` 参数 | `text.format` 参数 |
| 工具定义 | `tools[].function` | `tools[]`（支持 function + 内置工具） |
| 多轮开销 | 每次请求需发送完整历史，Token 消耗线性增长 | 引用 previous_response_id 复用缓存，缓存命中率提升 40-80% |
| 流式输出 | SSE `stream: true` | SSE `stream: true`（兼容） |

**请求参数（`POST /v1/responses`）：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `model` | string | 是 | 模型标识 |
| `input` | string \| array | 是 | 输入内容（纯文本或 Items 数组） |
| `instructions` | string | 否 | 系统级指令（替代 system message） |
| `tools` | array | 否 | 可用工具列表（function / web_search / file_search / code_interpreter / mcp） |
| `tool_choice` | string \| object | 否 | 工具选择策略（`auto` / `required` / `none` / 指定函数名） |
| `text.format` | object | 否 | 结构化输出配置（`{"type": "json_schema", "schema": {...}}`） |
| `store` | boolean | 否 | 是否在服务端存储此 Response（默认 true），用于多轮引用 |
| `previous_response_id` | string | 否 | 引用前一个 Response 的 ID，自动继承上下文 |
| `stream` | boolean | 否 | 是否启用 SSE 流式输出 |
| `temperature` | number | 否 | 采样温度 |
| `max_output_tokens` | integer | 否 | 最大输出 Token 数 |
| `top_p` | number | 否 | 核采样参数 |

**响应结构：**

```json
{
  "id": "resp_abc123",
  "object": "response",
  "model": "deepseek-ai/DeepSeek-R1",
  "created_at": 1712345678,
  "status": "completed",
  "output": [
    {
      "type": "message",
      "role": "assistant",
      "content": [
        {
          "type": "output_text",
          "text": "Transformer 架构是一种基于自注意力机制的神经网络架构..."
        }
      ]
    }
  ],
  "output_text": "Transformer 架构是一种基于自注意力机制的神经网络架构...",
  "usage": {
    "input_tokens": 42,
    "output_tokens": 256,
    "total_tokens": 298
  }
}
```

**请求示例 — Responses API：**

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.example.com/v1/",
    api_key="your-api-key"
)

# 单轮调用
response = client.responses.create(
    model="deepseek-ai/DeepSeek-R1",
    instructions="你是一个有帮助的助手",
    input="请解释什么是 Transformer 架构"
)
print(response.output_text)

# 多轮对话 — 通过 previous_response_id 链式引用
response2 = client.responses.create(
    model="deepseek-ai/DeepSeek-R1",
    input="它和 RNN 的区别是什么？",
    previous_response_id=response.id,
    store=True
)
print(response2.output_text)
```

**请求示例 — Chat Completions API（持续支持）：**

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.example.com/v1/",
    api_key="your-api-key"
)

response = client.chat.completions.create(
    model="deepseek-ai/DeepSeek-R1",
    messages=[
        {"role": "system", "content": "你是一个有帮助的助手"},
        {"role": "user", "content": "请解释什么是 Transformer 架构"}
    ],
    stream=True
)

for chunk in response:
    print(chunk.choices[0].delta.content, end="")
```

#### 4.1.2.2 Responses API 内置工具

Responses API 原生支持以下内置工具类型（通过 `tools` 参数配置）：

| 工具类型 | 说明 | 示例 |
|---------|------|------|
| `function` | 自定义函数调用（与 Chat Completions 的 Function Calling 对齐） | `{"type": "function", "name": "get_weather", "parameters": {...}}` |
| `web_search` | 内置联网搜索能力，模型可自动搜索并引用网络信息 | `{"type": "web_search"}` |
| `code_interpreter` | 内置代码解释器，模型可执行 Python 代码并返回结果 | `{"type": "code_interpreter"}` |
| `file_search` | 在向量存储中检索文件内容 | `{"type": "file_search", "vector_store_ids": ["vs_xxx"]}` |
| `mcp` | 连接远程 MCP 服务器，扩展模型外部能力 | `{"type": "mcp", "server_url": "https://..."}` |
| `image_generation` | 内置图像生成能力 | `{"type": "image_generation"}` |

**Items 类型体系**：Responses API 的 `input` 和 `output` 均使用 Items 作为基本数据单元。每个 Item 是一个类型化联合体：

| Item 类型 | 说明 |
|----------|------|
| `message` | 对话消息（role: user / assistant / developer） |
| `function_call` | 模型发起的函数调用（含函数名和参数） |
| `function_call_output` | 函数执行结果（由客户端填入） |
| `web_search_call` | 模型发起的联网搜索 |
| `file_search_call` | 模型发起的文件检索 |
| `code_interpreter_call` | 模型发起的代码执行 |
| `image_generation_call` | 模型发起的图像生成 |
| `mcp_call` | 模型发起的 MCP 工具调用 |

#### 4.1.3 模型 Flavor 机制

同一模型提供不同推理配置（Flavor），平衡性能与成本：

| Flavor | 特点 | 适用场景 | 使用方式 |
|--------|------|---------|---------|
| **Base** | 大 batch、高吞吐、标准延迟 | 批量处理、成本敏感场景 | 默认使用模型名 |
| **Fast** | 小 batch + 投机解码、低延迟 | 实时对话、交互应用 | 模型名追加 `-fast` 后缀 |

两种 Flavor 模型输出完全一致，差异仅在于 Token 定价和延迟表现。

#### 4.1.4 Service Tier 请求优先级

支持通过 `service_tier` 参数控制请求的优先级和计费策略：

| Tier | 说明 | 适用场景 |
|------|------|---------|
| **auto** | 自动选择最佳可用层级（默认值） | 通用场景 |
| **default** | 标准优先级，超出限流返回 429 | 生产环境实时请求 |
| **flex** | 低优先级处理，不消耗速率限制配额 | 非实时任务、成本敏感的批量作业 |

Flex Tier 的请求在平台空闲时处理，不占用用户的 RPM/TPM 配额，可显著降低非实时场景的使用成本。

#### 4.1.5 推理优化技术

**注意力与缓存优化**

| 技术 | 说明 |
|------|------|
| **PagedAttention** | 将 KV Cache 分页管理，减少显存碎片，支持更大并发 |
| **FlashAttention-4** | Blackwell 架构原生优化的注意力内核，5 级 warp 流水线设计，较 cuDNN 提速 1.3x，较 Triton 提速 2.7x；支持 FlexAttention 自定义注意力变体 |
| **FlashAttention-2/3** | 针对 Ampere/Hopper 架构的高效注意力计算内核，减少 HBM 读写 |
| **自动前缀缓存** | 相同 System Prompt 的 KV Cache 跨请求自动复用，零配置生效 |
| **KV Cache CPU 卸载** | 智能 CPU Offloading，仅存储高频复用的 KV Block；FlexKV 作为可选后端，支持多 KV Group |
| **KV Cache 压缩** | TurboQuant (Google) 实现 3-bit KV 存储，6x 内存压缩无精度损失；EchoKV 基于层间相似性重建 KV，支持全精度/压缩动态切换 |
| **语义缓存** | 基于 LanceDB 向量索引的语义相似查询缓存，语义相近的 Prompt 可复用已缓存的推理结果；LanceDB <1ms 最近邻查询确保缓存命中判定不增加额外延迟 |

**批处理与调度**

| 技术 | 说明 |
|------|------|
| **连续批处理** | 动态合批 (Continuous Batching)，请求即到即处理，最大化 GPU 利用率 |
| **Chunked Prefill** | 长 Prompt 分块预填充，避免单次 prefill 阻塞 decode |
| **Performance Mode** | vLLM 支持 `balanced` / `interactivity` / `throughput` 三种模式，一键切换延迟优先或吞吐优先策略 |

**量化推理**

| 技术 | 说明 |
|------|------|
| **FP8 量化** | Hopper/Blackwell 原生支持，精度损失 <1%，显存降低 ~2x |
| **NVFP4 量化** | Blackwell 原生 4-bit 浮点格式，两级缩放（FP8 block scale + FP32 tensor scale），显存降低 ~3.5x，精度接近 FP8 |
| **GPTQ / AWQ** | 社区主流的 INT4/INT8 权重量化方案 |
| **FP8+INT4 混合量化** | 国产 GPU 适配方案（寒武纪/昇腾），权重 INT4 + 激活 FP8，平衡精度与性能 |

**投机解码 (Speculative Decoding)**

| 技术 | 说明 |
|------|------|
| **Draft Model 投机** | 小模型预生成候选 Token，大模型单次前向验证，加速 1.4-2x |
| **EAGLE-3** | 在特征层面而非 Token 层面进行投机，专用投机层更高效 |
| **FastEagle** | 非自回归级联起草 — 单次前向生成整个 draft 序列（而非 N 次），进一步降低开销 |
| **N-gram 树投机** | 基于上下文 N-gram 模式匹配替代训练头，零参数训练，代码生成等重复性任务吞吐提升 2x |
| **GPU 原生投机** | vLLM v0.18 将 N-gram 投机迁移到 GPU 执行，兼容异步调度器，减少 CPU 开销 |

#### 4.1.6 分布式推理架构

**Prefill-Decode (PD) 分离**

Prefill 和 Decode 两个阶段具有截然不同的计算特性：Prefill 为计算密集型（GEMM），Decode 为内存带宽密集型（GEMV）。PD 分离将两阶段部署在不同物理节点，独立伸缩：

| 组件 | 说明 |
|------|------|
| **Prefill Worker** | 专注处理输入 Prompt 的 KV 计算，配置高算力 GPU，优化 TTFT |
| **Decode Worker** | 专注逐 Token 生成，配置大显存/高带宽 GPU，优化 TPOT |
| **KV 传输** | 通过 NIXL (NVIDIA Inference Xfer Library) 实现 Prefill→Decode 的 GPU 间直接 KV Cache 传输 |
| **KV-aware 路由** | 智能路由层感知已缓存的 KV 前缀，避免重复 Prefill 计算 |
| **NVIDIA Dynamo** | 数据中心级分布式推理编排框架（Rust 核心 + Python 扩展），支持 PD 分离、多引擎混合、SLA 驱动自动伸缩；兼容 vLLM / SGLang / TensorRT-LLM |

PD 分离在 DeepSeek-R1 等长推理模型上可实现 **最高 30x 吞吐提升**。

**MoE 专家并行 (Expert Parallelism)**

针对 DeepSeek-V4 (671B/37B 激活) 等 MoE 模型的专用优化：

| 技术 | 说明 |
|------|------|
| **弹性专家并行** | vLLM v0.18 支持动态 GPU 伸缩，按需加载/卸载专家权重 |
| **DeepEP 通信库** | DeepSeek 开源的 MoE 专用通信库，仅对激活的专家进行 All-to-All 通信，支持 FP8 精度 |
| **Dispatch/Combine 融合** | 通信与计算深度融合算子，端到端性能提升 20-25% |
| **Prefill EP + Decode EP** | Prefill 阶段使用较小 EP 度（如 EP32）+ TP/SP；Decode 阶段使用超大 EP 度（如 EP320）最小化延迟 |

**AF (Attention-FFN) 分离**（前沿方向）

在 PD 分离基础上更细粒度地将 Attention 和 FFN 模块部署在不同设备：Attention 节点处理注意力计算（需高带宽），FFN 节点处理前馈计算（可使用国产 GPU），打破对高端 GPU 的单一依赖。

#### 4.1.7 高级推理特性

| 特性 | 说明 |
|------|------|
| **Function Calling** | 模型可调用外部函数/工具，返回结构化调用参数；支持 `tool_choice` 参数（`auto` / 指定函数名）控制函数选择策略 |
| **结构化输出** | `json_object` 模式（任意 JSON）和 `json_schema` 模式（严格遵循指定 Schema），通过 `response_format` 参数控制 |
| **上下文缓存** | 长 System Prompt 级别的 KV Cache 持久化复用 |
| **Safety Guardrails** | 可插拔的输入/输出安全过滤模型 |

---

### 4.2 Playground 交互式试玩

Playground 提供基于 Web 的模型交互界面，面向零代码用户和快速实验场景。

#### 4.2.1 核心功能

| 功能 | 说明 |
|------|------|
| **模型对话** | 直接在浏览器中与模型对话，支持多轮上下文 |
| **System Prompt** | 编辑系统提示词，设置 few-shot 示例 |
| **参数调节** | 实时调节 temperature、top_p、max_tokens、频率惩罚等 |
| **多模态输入** | 支持上传图片进行视觉问答 |
| **模型对比** | 双窗口并排，同时向两个模型发送相同 Prompt 并对比结果 |
| **代码导出** | 一键导出 Python / JavaScript / cURL 代码片段 |
| **Function Calling 测试** | 在 Playground 中定义工具函数 Schema，测试模型的函数选择与参数生成能力 |
| **结构化输出测试** | 在参数面板中切换 Response Format（JSON Object / JSON Schema），验证模型的结构化输出效果 |
| **Prompt Preset** | 保存/加载/共享 Prompt 模板（含模型参数、System Prompt、Few-shot 示例），支持团队共享链接 |
| **对话历史** | 自动保存对话记录，支持导出 |

#### 4.2.2 模型卡片信息

每个模型在 UI 上展示以下信息：

- **输入 Token 价格** — USD/百万 Token
- **输出 Token 价格** — USD/百万 Token
- **生成速度** — 近似 Tokens/秒
- **质量评分** — MMLU 或其他 Benchmark 分数
- **上下文长度** — 最大支持的 Token 数
- **支持特性** — Function Calling / Vision / JSON Mode 等标签

---

### 4.3 专属端点 (Dedicated Endpoints)

专属端点为用户提供独立的推理部署实例，实现资源隔离和性能保障。

#### 4.3.1 核心概念

| 概念 | 说明 |
|------|------|
| **Template** | 可部署的模型"蓝图"，定义了支持的 Flavor、GPU 类型和区域 |
| **Flavor** | 模板变体（如 `base` / `fast`），不同的性能/成本特征 |
| **Endpoint** | 从 Template 创建的专属部署实例 |
| **endpoint_id** | 端点唯一标识，用于管理操作 |
| **routing_key** | 推理调用时的模型标识符，创建端点时返回 |

#### 4.3.2 管理接口 (Control Plane)

| 操作 | 接口 | 说明 |
|------|------|------|
| 查询模板列表 | `GET /v0/dedicated_endpoints/templates` | 查看可用的模型模板 |
| 创建端点 | `POST /v0/dedicated_endpoints` | 指定模型/GPU/区域/副本数 |
| 查询端点列表 | `GET /v0/dedicated_endpoints` | 查看已创建的所有端点 |
| 更新端点配置 | `PATCH /v0/dedicated_endpoints/{id}` | 修改副本数/GPU 等 |
| 删除端点 | `DELETE /v0/dedicated_endpoints/{id}` | 永久删除并释放资源 |

#### 4.3.3 创建端点参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 端点显示名称 |
| `description` | string | 否 | 端点描述 |
| `model_name` | string | 是 | 模板模型名 |
| `flavor_name` | string | 是 | `base` 或 `fast` |
| `gpu_type` | string | 是 | NVIDIA: `gpu-l40s` / `gpu-h100-sxm` / `gpu-h200` / `gpu-b200`；国产: `gpu-ascend-910b` / `gpu-ascend-950` / `gpu-hg-k100` / `gpu-cambricon-590` |
| `gpu_count` | integer | 是 | 每个副本的 GPU 数量 |
| `region` | string | 是 | 部署区域 |
| `scaling.min_replicas` | integer | 是 | 最小副本数（0 = 闲时释放） |
| `scaling.max_replicas` | integer | 是 | 最大副本数 |

#### 4.3.4 推理访问 (Data Plane)

- 使用创建时返回的 `routing_key` 作为 model 参数
- 复用标准 `/v1` OpenAI 兼容路由
- 根据端点部署区域选择对应的 Base URL，减少跨区延迟

#### 4.3.5 计费规则

- 端点在至少有一个副本运行时视为活跃并产生费用
- min_replicas = 0 时，无流量自动缩至零，停止计费
- 总 GPU = `gpu_count × 当前活跃副本数`

---

### 4.4 模型微调 (Fine-tuning)

微调功能允许用户在基础模型上使用自有数据进行训练，提升模型在特定领域的表现。

#### 4.4.1 训练方式

| 方式 | 说明 | 适用场景 |
|------|------|---------|
| **Full Fine-tuning** | 全参数训练，效果最佳 | 大量高质量数据、追求最佳效果 |
| **LoRA** | 低秩适配器，仅训练少量参数 | 数据量较少、快速迭代、成本敏感 |
| **长上下文微调** | 支持 32K+ Token 上下文训练 | 长文档理解、多轮对话优化 |

**即将支持：**
- 投机解码优化训练 — 训练专属 Draft Model 提升推理速度
- RLHF / DPO 强化学习 — 基于人类反馈/偏好数据的对齐训练

#### 4.4.2 训练任务管理

| 接口 | 说明 |
|------|------|
| `POST /v1/fine_tuning/jobs` | 创建微调任务 |
| `GET /v1/fine_tuning/jobs` | 查看所有微调任务 |
| `GET /v1/fine_tuning/jobs/{id}` | 查看任务详情 |
| `POST /v1/fine_tuning/jobs/{id}/cancel` | 取消任务 |
| `GET /v1/fine_tuning/jobs/{id}/events` | 查看训练事件日志 |
| `GET /v1/fine_tuning/jobs/{id}/checkpoints` | 查看 Checkpoint 列表 |

**任务生命周期**: `created` → `validating` → `queued` → `running` → `succeeded` / `failed` / `cancelled`

#### 4.4.3 数据集要求

**支持格式**：JSONL / CSV / Parquet

**对话格式 (ChatML)**：

```jsonl
{"messages": [{"role": "system", "content": "你是一个编程助手"}, {"role": "user", "content": "写一个快排"}, {"role": "assistant", "content": "..."}]}
{"messages": [{"role": "user", "content": "解释递归"}, {"role": "assistant", "content": "..."}]}
```

**数据集管理功能**：
- 文件上传（支持大文件分片 Multipart Upload）
- 自动格式验证与错误提示
- 在线预览数据集内容
- 版本管理

#### 4.4.4 LoRA 适配器管理

- 训练产出的 LoRA 权重独立存储和管理
- 提供在线合并脚本，将 LoRA 权重合并到 base model
- Serverless LoRA 推理 — 在共享推理集群上动态加载 LoRA，无需独占 GPU
- 同一 base model 支持多个 LoRA 适配器动态切换

---

### 4.5 数据实验室 (Data Lab)

Data Lab 是平台内的统一数据工作空间，集中管理推理日志、数据集和训练产出物。

#### 4.5.1 数据类型

| 数据类型 | 来源 | 说明 |
|---------|------|------|
| **推理日志** | API / Playground | 自动采集的 chat completion 记录（input/output/metadata） |
| **过滤数据集** | SQL 查询 | 从推理日志中通过 SQL 查询构建的子数据集 |
| **上传数据集** | 用户手动 | 用户自行上传的训练/评测数据集 |
| **批量推理输出** | Batch API | 批量推理任务的结果文件 |
| **微调产出物** | Fine-tuning | 训练产生的 Checkpoint 和模型文件 |

#### 4.5.2 核心功能

- **推理日志浏览** — 按模型/时间/项目/API Key 多维过滤和搜索
- **SQL 查询** — 使用 SQL 对推理日志进行查询和聚合，结果可保存为新数据集
- **数据集预览** — 在线查看数据集内容，支持分页和采样
- **导出** — 导出 CSV / JSONL / Lance 格式供离线使用
- **零数据留存** — 企业客户可开启 ZDR 模式，推理日志不做持久化
- **数据可复现** — 基于 Lance 格式的 Git-style 分支与版本管理（零拷贝克隆、不可变标签），保障微调实验的输入一致性
- **向量混合查询** — 推理日志和数据集支持向量搜索 + 全文搜索 + SQL 分析的混合查询（基于 LanceDB）

#### 4.5.3 Chat Completions 导入

支持将历史推理日志导入为结构化数据集：

1. 指定时间范围、模型筛选条件
2. 一键启动导入任务
3. 导入结果自动生成为 Data Lab 中的新数据集

导入受 ZDR 设置影响：ZDR 开启期间的日志不会被采集。

#### 4.5.4 Datasets API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/v1/datasets` | POST | 创建数据集（直接上传） |
| `/v1/datasets` | GET | 列出项目下所有数据集 |
| `/v1/datasets/{id}` | GET | 查询数据集详情 |
| `/v1/datasets/{id}` | PATCH | 更新数据集信息 |
| `/v1/datasets/{id}` | DELETE | 删除数据集 |
| `/v1/datasets/{id}/content` | GET | 读取数据集内容（分页） |
| `/v1/datasets/{id}/export` | GET | 导出为 CSV / JSONL |
| `/v1/datasets/{id}/query` | GET | 获取 SQL 查询模板 |
| `/v1/datasets/uploads` | POST | 创建大文件分片上传 |
| `/v1/datasets/uploads/{id}/parts` | POST | 上传分片 |
| `/v1/datasets/uploads/{id}/complete` | POST | 完成分片上传并创建数据集 |
| `/v1/datasets/operations` | GET | 列出数据集操作任务 |
| `/v1/datasets/operations/{id}` | GET | 查询操作状态 |
| `/v1/datasets/operations/{id}/results` | GET | 获取操作结果（Checkpoint / 输出文件） |

#### 4.5.5 数据处理与合规

- 所有数据处理在指定数据中心区域内完成
- 符合数据处理协议 (DPA) 条款
- 处理位置变更时提前通知用户，确保合规审查时间

---

### 4.6 批量推理 (Batch API)

针对无需实时响应的场景，提供异步批量推理能力。

#### 4.6.1 工作流程

1. **上传输入文件** — JSONL 格式，每行一个推理请求
2. **创建批量任务** — 指定模型、输入文件和完成窗口
3. **异步执行** — 平台利用空闲 GPU 资源在后台执行
4. **查询状态** — 轮询或 Webhook 获取任务进度
5. **下载结果** — 任务完成后下载输出文件

#### 4.6.2 核心优势

| 特性 | 说明 |
|------|------|
| **成本节省** | 价格为实时推理的 50%，利用空闲算力 |
| **更高限额** | 速率限制远高于实时推理 |
| **大规模处理** | 单任务支持数十万条请求 |
| **自动重试** | 失败请求自动重试，保障完成率 |

#### 4.6.3 管理接口

| 接口 | 说明 |
|------|------|
| `POST /v1/batches` | 创建批量任务 |
| `GET /v1/batches/{id}` | 查询任务状态 |
| `GET /v1/batches` | 列出所有批量任务 |
| `POST /v1/batches/{id}/cancel` | 取消任务 |

---

### 4.7 可观测性 (Observability)

提供实时和历史监控指标，帮助用户调试性能问题、基准测试模型、理解容量行为。

#### 4.7.1 指标体系

**流量指标**

| 指标 | 说明 |
|------|------|
| Requests Per Minute | 每分钟请求数 |
| Input Tokens Per Minute | 每分钟输入 Token 数 |
| Output Tokens Per Minute | 每分钟输出 Token 数 |
| Input Tokens Per Request | 请求 Prompt 大小分布 |
| Output Tokens Per Request | 响应生成长度分布 |

**延迟指标**（P50 / P90 / P99 分位数）

| 指标 | 说明 |
|------|------|
| End-to-End Latency | 从请求发送到完整响应接收的总延迟 |
| TTFT (Time to First Token) | 首个 Token 生成延迟 |
| TPS (Tokens Per Second) | 每秒生成 Token 数 |

**伸缩与容量**

| 指标 | 说明 |
|------|------|
| Active Replicas | 当前运行中的副本数 |
| GPU Utilization | GPU 计算利用率 |
| GPU Memory Usage | 显存使用水位 |

**错误率**

| 指标 | 说明 |
|------|------|
| Error Rate by Status Code | 按 HTTP 状态码分组的错误率 |
| 429 Rate Limit Hits | 限流触发次数 |

#### 4.7.2 过滤维度

支持按以下维度交叉过滤所有指标：

- 时间范围、模型 / 端点、项目、API Key、区域、错误码、Prompt 长度、延迟区间

#### 4.7.3 外部集成

- **Prometheus** — 标准 metrics 端点导出，兼容现有监控栈
- **Grafana** — 预配置仪表盘模板，支持自定义可视化
- **Webhook** — 自定义告警规则与通知渠道

---

### 4.8 团队管理与权限控制

#### 4.8.1 组织层级

```
Organization (组织)
├── Billing Account (计费账户)
├── Members (成员管理)
├── SSO Configuration (单点登录)
├── Project A (项目 A)
│   ├── API Keys
│   ├── Models & Endpoints
│   ├── Datasets
│   ├── Fine-tuning Jobs
│   └── Observability
└── Project B (项目 B)
    └── ...
```

#### 4.8.2 角色权限 (RBAC)

| 角色 | 范围 | 权限 |
|------|------|------|
| **Org Admin** | 组织级 | 全局管理：成员、计费、所有项目 |
| **Billing Manager** | 组织级 | 仅管理计费与账单 |
| **Project Admin** | 项目级 | 管理项目配置、成员、资源 |
| **Project Member** | 项目级 | 使用项目资源（推理、微调等） |
| **Custom Role** | 自定义 | 按需配置细粒度权限组合 |

#### 4.8.3 认证方式

| 方式 | 说明 |
|------|------|
| 账号密码 | 基础登录方式 |
| OAuth 2.0 | 支持 Google / GitHub 社交登录 |
| SAML 2.0 | 企业 IdP 单点登录 (Okta, Azure AD 等) |
| OIDC | OpenID Connect 协议支持 |
| MFA | 多因素认证 (TOTP / WebAuthn) |

#### 4.8.4 API Key 管理

- 项目级别创建与管理
- 可配置权限范围（推理 / 微调 / 管理 / 只读）
- 设置过期时间
- 使用量按 Key 维度统计
- 泄露自动检测（GitHub Secret Scanning 集成）与轮换通知

---

### 4.9 计费系统

#### 4.9.1 计费模式

| 服务 | 计费方式 | 说明 |
|------|---------|------|
| **共享推理** | Token 计价 | 输入/输出 Token 分别定价（USD/百万 Token） |
| **专属端点** | GPU·时 | 副本数 × GPU 数量 × 运行时间 |
| **批量推理** | Token 计价 (50% 折扣) | 与共享推理相同定价的一半 |
| **微调训练** | GPU·时 + 训练 Token | 训练使用的 GPU 时间 + 处理的 Token 数 |
| **存储** | GB·月 | 数据集、模型文件、checkpoint 的存储费用 |

#### 4.9.2 支付方式

| 方式 | 说明 | 适用 |
|------|------|------|
| 支付宝 | 扫码支付 / 网页支付，实时到账；支持自动代扣（签约免密支付） | 所有用户 |
| 微信支付 | 扫码支付 / JSAPI / H5 支付，实时到账；支持微信自动扣款 | 所有用户 |
| 余额充值 | 手动预充值余额，通过支付宝/微信支付完成充值，到账即时生效 | 所有用户 |
| 银行转账 | 按月结算，由销售团队开具形式发票 | 企业客户（仅公司） |
| Promo Code | 推广码充值，提供免费平台积分，具有有效期限制；不可用于支付逾期账单 | 营销活动 / 试用 |

**新用户试用**：注册即赠送试用积分（如 $1），有效期 30 天。

#### 4.9.3 用量监控

- 按项目 / 模型 / API Key 维度的消费面板
- 实时余额与消费额展示
- 预算告警（设定阈值，邮件/Webhook 通知）
- 历史趋势图表与 CSV 导出
- 费用预估器（输入预期用量，估算月度费用）

#### 4.9.4 速率限制与动态伸缩

平台采用动态速率限制机制，自动适配用户流量增长：

- **默认限额**：每个用户/项目有 RPM（请求/分钟）和 TPM（Token/分钟）基础限额
- **自动扩容**：15 分钟滚动窗口内平均使用率 ≥ 80% 时，限额自动提升 20%（×1.2）
- **自动缩容**：使用率 ≤ 50% 时，限额自动降低（÷1.5）
- **硬上限**：最大可扩展至基础限额的 20 倍
- **超限处理**：系统有空闲算力时，超限请求可能以低优先级被处理（响应头 `x-ratelimit-over-limit: yes`）
- **429 响应**：超限时返回 HTTP 429，附带 `Retry-After` 头

**响应头信息**：

| Header | 说明 |
|--------|------|
| `x-ratelimit-limit-requests` | 当前 RPM 限额 |
| `x-ratelimit-limit-tokens` | 当前 TPM 限额 |
| `x-ratelimit-remaining-requests` | 剩余可用请求数 |
| `x-ratelimit-remaining-tokens` | 剩余可用 Token 数 |
| `x-ratelimit-dynamic-scale-*` | 当前动态伸缩倍率 |
| `Retry-After` | 限流后需等待的秒数 |

---

### 4.10 第三方集成

#### 4.10.1 API 路由与代理

| 集成 | 说明 |
|------|------|
| OpenRouter | 统一 API 路由接入 |
| LiteLLM | 多模型代理层 |
| Portkey | AI Gateway 与可观测性 |

#### 4.10.2 应用框架与 API

| 框架 | 类型 |
|------|------|
| LangChain | LLM 应用开发框架 |
| LlamaIndex | RAG 与数据框架 |
| Pydantic AI | 结构化输出框架 |
| AIsuite | 统一多模型 API 抽象层 |
| Hugging Face | 模型推理 Client 兼容 |

#### 4.10.3 Agent 框架

| 框架 | 说明 |
|------|------|
| CrewAI | 多 Agent 协作框架 |
| Agno | Agent 编排工具 |
| Camel AI | 角色扮演 Agent |
| Google ADK | Google Agent 开发套件 |
| Strands | 轻量 Agent 框架 |

#### 4.10.4 Coding Assistants

| 工具 | 说明 |
|------|------|
| Cursor | AI 代码编辑器集成 |
| Continue | VS Code AI 助手 |
| Cline | CLI AI 助手 |
| Kilo Code | AI 代码生成助手 |
| VS Code | VS Code Copilot 兼容接入 |
| Zed | 高性能编辑器 AI 集成 |

#### 4.10.5 开发者工具

| 工具 | 说明 |
|------|------|
| Postman | API 测试工具 |
| Linkup | 外部数据连接工具 |

#### 4.10.6 国产生态集成

| 工具/平台 | 说明 |
|---------|------|
| 魔搭 ModelScope | 阿里达摩院模型社区，国产模型下载与托管 |
| 玄武 XuanWu | 国产算力大模型推理框架 |
| LMDeploy | 书生团队部署工具，多国产 GPU 适配 |
| DashScope SDK | 阿里云百炼 Python/Java SDK |
| 钉钉/飞书/企微 | 国产 IM 平台 Webhook 告警与 Bot 集成 |

#### 4.10.7 可观测性平台

| 平台 | 说明 |
|------|------|
| Helicone | LLM 调用日志与分析 |
| Keywords AI | AI 应用监控 |

---

### 4.11 迁移指南 (Switch Guide)

提供从主流推理平台（OpenAI、其他兼容服务）零成本迁移的完整指引：

#### 4.11.1 迁移步骤

1. **修改 `base_url`** — 将请求基础地址指向本平台 API 端点
2. **设置 API Key** — 使用平台签发的 API Key 替换原有密钥
3. **指定模型名** — 使用平台支持的模型标识（如 `deepseek-ai/DeepSeek-R1`）

除此之外无需任何代码改动，完全兼容 OpenAI SDK（Python / JavaScript / cURL）。

#### 4.11.2 迁移示例

```python
from openai import OpenAI
import os

client = OpenAI(
    base_url="https://api.example.com/v1/",
    api_key=os.environ.get("API_KEY")
)

response = client.chat.completions.create(
    model="deepseek-ai/DeepSeek-R1",
    messages=[
        {"role": "system", "content": "You are a helpful assistant"},
        {"role": "user", "content": "Hello!"}
    ]
)
print(response.to_json())
```

#### 4.11.3 兼容性保障

| 特性 | 兼容说明 |
|------|---------|
| **Responses API** | 完全兼容，支持 Items 模型、`previous_response_id` 多轮引用、`instructions` 顶层指令、内置工具 |
| Chat Completions API | 完全兼容，含 SSE 流式输出 |
| Completions API (FIM) | 完全兼容 |
| Embeddings API | 完全兼容 |
| Function Calling | 完全兼容 `tools` / `tool_choice` 参数（Responses API 和 Chat Completions 均支持） |
| Structured Output | Chat Completions: `response_format` 参数；Responses API: `text.format` 参数 |
| Files & Batches API | 完全兼容 |
| Fine-tuning API | 完全兼容 |

---

### 4.12 Cookbook 开发者示例库

提供官方维护的开源示例代码仓库（GitHub），降低开发者上手门槛：

| 示例分类 | 内容 |
|---------|------|
| **API 调用示例** | Chat Completions、Embeddings、Rerank、Image Generation 各语言完整示例 |
| **Agent 框架集成** | Agno、Camel AI、CrewAI、Google ADK、LlamaIndex、Pydantic、Strands 等框架示例 |
| **RAG 管道** | 文档问答、向量数据库集成、端到端 RAG Pipeline |
| **微调与蒸馏** | LoRA 微调、模型蒸馏（大模型→小模型知识迁移）完整流程 |
| **内容生成** | 社交媒体文案、Demo App 等应用场景示例 |
| **多模态** | 视觉问答、图像生成等多模态用例 |

Cookbook 同时作为平台功能的实践参考和新用户 Onboarding 教程。

---

### 4.13 CLI 命令行工具

提供官方命令行客户端，面向开发者和 DevOps 用户，实现终端环境下的全平台操作能力。CLI 作为 Web Console 和 SDK 的补充，特别适合自动化脚本、CI/CD 流水线、远程服务器操作等场景。

#### 4.13.1 CLI 设计原则

| 原则 | 说明 |
|------|------|
| OpenAI CLI 兼容 | 命令结构参考 OpenAI CLI 规范，降低学习成本；已有 OpenAI CLI 脚本可低成本迁移 |
| 交互式 + 脚本化双模式 | 支持交互式 TUI（彩色输出、进度条、确认提示）和脚本化模式（`--json` 输出、`--quiet` 静默、`--yes` 跳过确认） |
| UNIX 哲学 | 支持 stdin/stdout 管道组合，输出可直接被 `jq`, `grep`, `awk` 等工具处理 |
| 渐进式认证 | `login` 交互式登录 / `--api-key` 参数传入 / `INFERENCE_API_KEY` 环境变量 / 配置文件 `~/.config/inference/config.yaml` |

#### 4.13.2 核心命令体系

```bash
inference <resource> <action> [flags]
```

| 命令组 | 核心命令 | 说明 |
|-------|---------|------|
| **认证** | `inference auth login` | 交互式登录，获取并保存 API Key |
| | `inference auth status` | 查看当前认证状态和配额 |
| | `inference auth switch --profile <name>` | 多环境 / 多账号切换 |
| **模型** | `inference models list` | 列出所有可用模型（支持 `--type`, `--provider` 过滤） |
| | `inference models info <model>` | 查看模型详情（参数量、定价、能力标签、支持的 Flavor） |
| **推理** | `inference chat -m <model>` | 进入交互式对话（支持流式输出、多轮上下文） |
| | `inference chat -m <model> -p "prompt"` | 单次推理调用 |
| | `inference chat -m <model> --file input.jsonl` | 从文件读取输入批量调用 |
| | `inference responses create -m <model> -i "input"` | Responses API 调用 |
| | `inference completions create -m <model> -p "prompt"` | Completions API 调用 |
| | `inference embeddings create -m <model> -i "text"` | 生成向量嵌入 |
| **批量推理** | `inference batches create --file <file> -m <model>` | 创建批量推理任务 |
| | `inference batches list` | 列出批量任务 |
| | `inference batches status <batch_id>` | 查看任务进度 |
| | `inference batches download <batch_id> -o result.jsonl` | 下载批量结果 |
| **专属端点** | `inference endpoints list` | 列出所有专属端点 |
| | `inference endpoints create --template <tpl> --name <name>` | 创建专属端点 |
| | `inference endpoints scale <endpoint> --min 1 --max 4` | 调整伸缩策略 |
| | `inference endpoints logs <endpoint> --follow` | 实时查看端点日志 |
| | `inference endpoints delete <endpoint>` | 删除端点 |
| **微调** | `inference finetune create -m <base> --train data.jsonl` | 创建微调任务 |
| | `inference finetune list` | 列出微调任务 |
| | `inference finetune status <job_id>` | 查看训练进度和指标 |
| | `inference finetune cancel <job_id>` | 取消训练任务 |
| **数据集** | `inference datasets upload <file>` | 上传数据集（支持 CSV/JSONL，自动分片大文件） |
| | `inference datasets list` | 列出数据集 |
| | `inference datasets query <id> --sql "SELECT ..."` | SQL 查询数据集 |
| | `inference datasets export <id> -o output.csv` | 导出数据集 |
| **文件** | `inference files upload <file>` | 上传文件 |
| | `inference files list` | 列出已上传文件 |
| | `inference files delete <file_id>` | 删除文件 |
| **API Key** | `inference api-keys list` | 列出所有 API Key |
| | `inference api-keys create --name <name>` | 创建新 Key |
| | `inference api-keys revoke <key_id>` | 吊销 Key |
| **用量** | `inference usage summary` | 当前月用量和余额概览 |
| | `inference usage detail --from 2026-03-01 --to 2026-03-31` | 指定日期范围的详细用量 |
| **配置** | `inference config set default_model <model>` | 设置默认模型 |
| | `inference config set output json` | 设置默认输出格式 |
| | `inference config view` | 查看当前配置 |

#### 4.13.3 高级特性

| 特性 | 说明 |
|------|------|
| **流式交互模式** | `inference chat` 进入 REPL，支持方向键历史、Tab 补全模型名、`/system` 设置系统指令、`/clear` 清除上下文、`/export` 导出对话 |
| **管道与组合** | `echo "翻译成英文: 你好" \| inference chat -m qwen3.5` 支持 stdin 输入；`--json` 输出结构化 JSON 方便 `jq` 处理 |
| **并行批量调用** | `inference chat --file prompts.jsonl --concurrency 10 --output results.jsonl` 多并发批量推理 |
| **自动重试** | 内置指数退避重试（429/5xx），`--max-retries` 可配置 |
| **进度追踪** | 微调、批量推理等长时间任务支持 `--wait` 阻塞等待完成，带进度条和 ETA |
| **输出格式** | `--format table\|json\|yaml\|csv` 多种输出格式；`--no-color` 禁用颜色 |
| **Shell 补全** | `inference completion bash\|zsh\|fish\|powershell` 生成各 Shell 的自动补全脚本 |
| **自动更新** | `inference update` 自检新版本并自动更新；支持 `--check` 仅检查 |
| **多配置文件** | `~/.config/inference/config.yaml` 支持多 Profile，对应不同环境（dev/staging/prod）或不同账号 |

#### 4.13.4 安装分发

| 分发渠道 | 命令 |
|---------|------|
| Homebrew (macOS/Linux) | `brew install inference-cli` |
| npm | `npm install -g @inference/cli` |
| pip | `pip install inference-cli` |
| Docker | `docker run --rm -it inference/cli chat -m deepseek-r1` |
| 二进制下载 | GitHub Releases 提供 Linux/macOS/Windows (amd64/arm64) 预编译二进制 |
| Shell 一键安装 | `curl -fsSL https://cli.example.com/install.sh \| sh` |

#### 4.13.5 CI/CD 集成示例

```yaml
# GitHub Actions 示例
- name: Run batch inference
  env:
    INFERENCE_API_KEY: ${{ secrets.INFERENCE_API_KEY }}
  run: |
    inference batches create \
      --file test-prompts.jsonl \
      --model deepseek-ai/DeepSeek-R1 \
      --wait \
      --output results.jsonl
    inference datasets upload results.jsonl --name "ci-eval-${{ github.sha }}"
```

---

## 5. 系统架构

### 5.1 总体架构分层

系统采用云原生微服务架构，分为 7 个层次：

```
┌─────────────────────────────────────────────────────────────┐
│                     接入层 (Access Layer)                     │
│  ┌──────────┐ ┌──────────────────┐ ┌─────────┐ ┌─────────┐ │
│  │Web Console│ │Responses API +   │ │SDK      │ │CLI 工具  │ │
│  │(React)    │ │Chat Completions  │ │Py/JS/Go │ │         │ │
│  └──────────┘ └──────────────────┘ └─────────┘ └─────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                 网关与路由层 (Gateway & Routing)               │
│  ┌──────────┐ ┌──────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │
│  │API GW    │ │Responses │ │认证鉴权 │ │速率限制 │ │智能路由 │ │
│  │Kong/Envoy│ │Adapter   │ │JWT/Key │ │引擎    │ │Router  │ │
│  └──────────┘ └──────────┘ └────────┘ └────────┘ └────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                     控制面 (Control Plane)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ ┌─────┐ │
│  │用户/团队  │ │计费服务   │ │端点管理   │ │模型注册 │ │微调  │ │
│  │服务      │ │          │ │服务      │ │中心    │ │调度  │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ └─────┘ │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│               数据面 (Data Plane) — GPU 集群                  │
│  ┌──────────────┐ ┌────────────┐ ┌──────────┐ ┌──────────┐ │
│  │推理引擎集群   │ │专属端点     │ │批量推理   │ │微调训练   │ │
│  │(vLLM 共享池) │ │实例池      │ │工作节点   │ │集群      │ │
│  └──────────────┘ └────────────┘ └──────────┘ └──────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  调度与编排层 (Orchestration)                  │
│  ┌────────────────┐ ┌──────────────┐ ┌──────────────────┐   │
│  │Kubernetes +    │ │HPA / KEDA    │ │Volcano / Kueue   │   │
│  │GPU Operator    │ │自动伸缩      │ │GPU 调度器         │   │
│  └────────────────┘ └──────────────┘ └──────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                 存储与数据层 (Storage & Data)                  │
│  ┌──────────┐ ┌───────┐ ┌───────┐ ┌──────────┐ ┌───────┐ ┌────────┐ │
│  │PostgreSQL│ │Redis  │ │S3     │ │ClickHouse│ │Kafka  │ │LanceDB │ │
│  │(元数据)  │ │(缓存) │ │(对象) │ │(分析)    │ │(事件) │ │(AI数据)│ │
│  └──────────┘ └───────┘ └───────┘ └──────────┘ └───────┘ └────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   可观测性 (Observability)                    │
│  ┌──────────────┐ ┌─────────┐ ┌────────────────┐ ┌───────┐ │
│  │Prometheus +  │ │Grafana  │ │OpenTelemetry + │ │Loki + │ │
│  │VictoriaMetrics│ │仪表盘   │ │Jaeger 追踪     │ │日志   │ │
│  └──────────────┘ └─────────┘ └────────────────┘ └───────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 推理请求数据流

```
Client Request (HTTPS) — POST /v1/responses 或 POST /v1/chat/completions
  └─→ API Gateway (TLS 终止, 请求解析)
      ├─→ Auth Service ── Redis 缓存验证 API Key
      │   └─ 失败 → 401 Unauthorized
      ├─→ Rate Limiter ── Redis 滑动窗口计数器
      │   └─ 超限 → 429 Too Many Requests + Retry-After
      ├─→ Responses Adapter (仅 /v1/responses 路径触发)
      │   ├─→ Items → Messages 转换 (input 解析为内部推理格式)
      │   ├─→ previous_response_id → 从 Response Store 加载历史上下文
      │   ├─→ instructions → system message 注入
      │   └─→ 内置工具 (web_search / code_interpreter 等) → 工具编排
      └─→ Model Router
          ├─→ routing_key 匹配 → 专属端点实例 (Dedicated)
          └─→ 模型名匹配 → 共享推理集群 (Shared Pool)
              └─→ Load Balancer → vLLM Worker (GPU)
                  ├─→ Prefill (输入处理)
                  ├─→ Decode (Token 逐个生成)
                  ├─→ SSE Stream → Client (流式返回)
                  ├─→ Response Store (store=true 时持久化 Response 对象)
                  └─→ 异步写入 Kafka
                      ├─→ ClickHouse (推理日志, Data Lab)
                      └─→ Billing Service (Token 计量扣费)
```

### 5.3 批量推理数据流

```
Client: POST /v1/batches (input_file_id, model)
  └─→ Batch Service
      ├─→ 验证输入文件格式 (从 S3 读取)
      ├─→ 创建 Temporal Workflow
      │   ├─→ 拆分子任务 (按行分片, 每片 N 条)
      │   ├─→ 提交到推理队列 (低优先级)
      │   ├─→ vLLM Worker Pool 执行 (利用空闲 GPU)
      │   ├─→ 结果聚合 → 输出文件 → S3
      │   └─→ 更新任务状态 → PostgreSQL
      └─→ Client: GET /v1/batches/{id} (轮询状态)
          └─→ completed → 下载输出文件
```

### 5.4 微调训练数据流

```
Client: POST /v1/fine_tuning/jobs
  └─→ Fine-tuning Service
      ├─→ 验证数据集格式 (Data Validator)
      ├─→ 创建 Temporal Workflow
      │   ├─→ 资源分配 (Volcano/Kueue → GPU 节点)
      │   ├─→ 拉取 Base Model (S3 / Model Registry)
      │   ├─→ 拉取训练数据集 (S3)
      │   ├─→ 执行训练 (PyTorch + DeepSpeed / FSDP)
      │   │   ├─→ Checkpoint 定期保存 → S3
      │   │   ├─→ 训练指标 (loss, lr, step) → Kafka → DB
      │   │   └─→ 评估指标 (validation loss) → DB
      │   ├─→ 训练完成 → 最终模型权重 → S3
      │   └─→ 注册模型 → Model Registry
      └─→ 可一键部署为推理端点 (Shared / Dedicated)
```

### 5.5 自动伸缩机制

#### 共享推理集群伸缩

```
KEDA ScaledObject / Custom HPA Controller
  └─→ 监控指标源
      ├─→ 推理请求队列深度 (Pending Requests in Queue)
      ├─→ GPU 利用率 (DCGM Exporter → Prometheus)
      └─→ RPM/TPM 使用率 (Rate Limiter Metrics)
  └─→ 伸缩决策
      ├─→ Scale Up: 创建新 Pod → 调度 GPU → 加载模型 → 就绪服务
      ├─→ Scale Down: 等待在途请求完成 → 优雅终止 Pod → 释放 GPU
      └─→ Cluster Autoscaler: 节点级扩容/缩容 (GPU 节点池)
```

#### 专属端点伸缩

```
Endpoint Autoscaler (per-endpoint)
  └─→ 监控该端点的请求队列和延迟
  └─→ 基于 min_replicas / max_replicas 配置
      ├─→ 请求增加 → 增加副本 (直到 max)
      ├─→ 请求减少 → 减少副本 (直到 min)
      └─→ min=0 且无流量 → 缩至零 (冷启动延迟约 30-120 秒)
```

---

## 6. 技术选型

### 6.1 推理引擎层

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| LLM 推理 | **vLLM (v0.18+)** | 业界标准开源推理引擎；Model Runner V2 GPU 原生输入准备、异步调度流水线（E2E 吞吐提升 30%+）；内置 PagedAttention、FlashAttention-4、连续批处理、EAGLE/N-gram GPU 投机解码、弹性专家并行、KV Cache CPU 卸载、Performance Mode 一键切换 |
| 多模态推理 | **vLLM + SGLang** | vLLM 已原生支持 VLM 模型；SGLang 在多模态和结构化生成场景有更优吞吐，可作为补充引擎 |
| 分布式编排 | **NVIDIA Dynamo** | 数据中心级分布式推理框架（Rust 核心），支持 PD 分离、KV-aware 路由、NIXL GPU 间 KV 直传、SLA 驱动自动伸缩；兼容 vLLM/SGLang/TRT-LLM |
| 注意力内核 | **FlashAttention-4 / FlexAttention** | FA4 为 Blackwell 原生优化（1.3x cuDNN），CuTe-DSL 实现编译快 20-30x；FlexAttention 支持自定义注意力变体（score_mod/mask_mod）零 CUDA 知识 |
| 图像/视频生成 | **Diffusers + ComfyUI** | HuggingFace Diffusers 标准化加载 Flux/SDXL/CogView 等模型；ComfyUI 提供灵活工作流编排；支持 NVFP4 量化 |
| 模型格式 | **Safetensors / GGUF** | Safetensors 安全高效的权重序列化格式；GGUF 用于量化模型 |
| MoE 通信 | **DeepEP** | DeepSeek 开源的 MoE 专用通信库，仅对激活专家进行 All-to-All 通信，支持 FP8 精度和跨机带宽优化 |
| 国产 GPU 推理 | **vLLM-Ascend / LMDeploy** | vLLM-Ascend 为昇腾适配的 vLLM 分支；LMDeploy (书生团队) 全面支持昇腾/寒武纪/海光等国产 GPU，提供 FP8+INT4 混合量化推理 |
| GPU 监控 | **DCGM Exporter / CANN Monitor** | NVIDIA 使用 DCGM Exporter；华为昇腾使用 CANN 生态的 NPU 指标导出器 |

### 6.2 后端服务层

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| API 服务 | **Go (Gin / Echo)** | 高并发低延迟，协程模型天然适合网关和代理场景；静态编译，容器镜像小 |
| Responses Adapter | **Go 微服务** | Responses API 协议适配层：Items ↔ Messages 双向转换、`previous_response_id` 上下文加载、`instructions` 注入、内置工具编排；与 Chat Completions 共享底层推理引擎 |
| Response Store | **PostgreSQL + Redis** | 持久化存储 Response 对象（`store: true`），支持通过 `previous_response_id` 查询历史上下文；Redis 缓存热 Response 加速多轮引用 |
| 异步工作流 | **Temporal** | 可靠的分布式工作流引擎，管理微调任务、批量推理、数据处理等长时间运行的有状态任务流；内置重试、补偿、可视化 |
| 消息队列 | **Apache Kafka** | 推理日志采集、计费事件流、系统间异步通信；高吞吐、持久化、消费者组 |
| API Gateway | **Kong / Envoy** | Kong 提供完善的插件生态（认证/限流/日志/变换）；Envoy 在 gRPC/HTTP2 和 L7 负载均衡场景性能更优，可与 Istio 集成 |
| 任务队列 | **Redis + Bull (Node) / Celery (Python)** | 轻量级任务队列，处理文件上传后处理、通知发送等短时异步任务 |

### 6.3 前端技术

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| Web 框架 | **React 19 + Next.js 15** | SSR 首屏渲染 + CSR 交互体验；App Router 支持 Server Components |
| UI 组件 | **shadcn/ui + TailwindCSS** | 无需额外 CSS-in-JS 运行时；组件源码可控，深度定制无阻碍 |
| Playground 编辑器 | **Monaco Editor** | VS Code 同款编辑器内核，JSON/代码编辑体验一流 |
| 流式渲染 | **SSE (Server-Sent Events)** | 原生浏览器支持，实现 Token 逐字流式显示 |
| 图表 | **Recharts / Apache ECharts** | Recharts 轻量适合仪表盘，ECharts 复杂可视化能力更强 |
| 状态管理 | **Zustand / TanStack Query** | Zustand 全局状态轻量；TanStack Query 管理服务端数据缓存 |

### 6.3.1 CLI 工具技术

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 开发语言 | **Go** | 编译为单个静态二进制，跨平台零依赖分发（Linux/macOS/Windows, amd64/arm64）；与后端服务同栈，共享 SDK 和类型定义 |
| CLI 框架 | **Cobra + Viper** | Cobra 是 Go 生态最成熟的 CLI 框架（kubectl/gh/docker 均采用），子命令/Flag/补全/Help 一体化；Viper 管理多层配置（文件/环境变量/Flag 优先级） |
| TUI 交互 | **Bubble Tea + Lip Gloss** | Charm 生态的 TUI 框架，用于交互式对话模式、进度条、表格渲染、spinner 等丰富终端 UI |
| HTTP 客户端 | **平台 Go SDK (自建)** | CLI 直接复用平台 Go SDK，确保 API 调用逻辑与服务端 SDK 一致 |
| 输出格式化 | **tablewriter + encoding/json** | 表格格式 (默认人类友好) / JSON / YAML / CSV 多格式输出 |
| 自动更新 | **go-selfupdate** | GitHub Releases 自检新版本并原地替换二进制 |
| 分发打包 | **GoReleaser** | 一键构建多平台二进制 + Homebrew Tap + Docker Image + Chocolatey |
| Shell 补全 | **Cobra 内置** | 原生支持 Bash/Zsh/Fish/PowerShell 补全脚本生成 |

### 6.4 存储方案

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 关系型数据库 | **PostgreSQL 16** | 用户/项目/模型/任务元数据存储；JSONB 支持半结构化数据；成熟稳定，生态丰富 |
| 缓存 | **Redis 7 Cluster** | API Key 验证缓存、速率限制滑动窗口计数器、session 管理、分布式锁 |
| 对象存储 | **S3** | 模型权重文件（数 GB-数百 GB）、数据集、训练 Checkpoint、批量推理结果 |
| 分析数据库 | **ClickHouse** | 推理日志、计费明细、可观测性指标的高速写入与分析查询；列式存储，压缩率高 |
| 搜索引擎 | **Meilisearch** | 模型目录搜索、文档检索；轻量易部署，Typo-tolerant 搜索 |
| AI 数据格式 | **Lance (v4.0+)** | 面向 AI/ML 的列式数据格式，随机访问比 Parquet 快 100x；原生支持向量列 + 多模态数据（文本/图像/音视频/嵌入）混合存储；零拷贝 Schema 演化，Git-style 分支与版本管理；Data Lab 数据集和推理日志的底层存储格式 |
| 向量数据库 | **LanceDB** | 基于 Lance 格式的向量数据库，<1ms 最近邻查询；支持向量搜索 + 全文搜索 + SQL 分析的混合查询；用于语义缓存、Embedding 存储、file_search 向量索引；嵌入式 / 托管 / BYOC 部署模式；已通过 SOC2 Type II + HIPAA 认证 |

### 6.5 基础设施与编排

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 容器编排 | **Kubernetes 1.30+** | GPU 集群统一编排，行业标准 |
| GPU 管理 (NVIDIA) | **NVIDIA GPU Operator** | 自动管理 GPU 驱动、CUDA 运行时、Device Plugin；支持 MIG (Multi-Instance GPU) |
| GPU 管理 (国产) | **Ascend Device Plugin / HAMi** | 华为昇腾 Device Plugin 管理 NPU 资源分配；HAMi (异构 AI 计算虚拟化中间件) 提供国产 GPU 统一抽象层，支持昇腾/海光/寒武纪等异构 GPU 混合调度 |
| GPU 调度 | **Volcano / Kueue** | GPU 作业队列管理，支持抢占调度、公平调度、Gang Scheduling（多 GPU 训练任务） |
| 自动伸缩 | **KEDA + Cluster Autoscaler** | KEDA 基于自定义指标（队列深度/GPU 利用率）驱动 Pod 伸缩；Cluster Autoscaler 驱动节点伸缩 |
| 服务网格 | **Istio / Linkerd** | 内部服务间 mTLS、流量管理、可观测性（可选） |
| IaC | **Terraform + Pulumi** | 基础设施即代码，多云/多区域资源编排 |
| CI/CD | **GitHub Actions + ArgoCD** | GitOps 工作流，自动化构建和部署 |
| 容器镜像 | **Harbor** | 私有镜像仓库，存储推理引擎和模型镜像 |

### 6.6 可观测性工具链

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 指标采集 | **Prometheus + VictoriaMetrics** | Prometheus 生态兼容指标采集；VictoriaMetrics 提供高效长期存储，资源消耗低于原生 Prometheus |
| 可视化 | **Grafana** | 统一仪表盘，支持 Prometheus / ClickHouse / Loki 等多数据源；同时作为平台内嵌 Observability 的后端 |
| 分布式追踪 | **OpenTelemetry + Jaeger** | OTel SDK 采集 trace/metrics/logs，Jaeger 提供 trace 可视化；端到端请求链路追踪 |
| 日志聚合 | **Loki + Promtail** | 结构化日志聚合，与 Grafana 原生集成，LogQL 查询语法 |
| 告警 | **Grafana Alerting + PagerDuty** | 基于指标/日志的告警规则，多渠道通知（邮件/Slack/钉钉/Webhook） |

---

## 7. 安全与合规

### 7.1 传输安全

| 措施 | 说明 |
|------|------|
| TLS 1.3 | 所有外部通信全链路加密 |
| mTLS | API Gateway 与内部服务之间双向 TLS（通过 Service Mesh） |
| Certificate Rotation | 证书自动轮换（cert-manager） |

### 7.2 数据安全

| 措施 | 说明 |
|------|------|
| 静态加密 | S3 SSE-KMS (AES-256) 加密存储的模型和数据 |
| 零数据留存 (ZDR) | 企业客户可开启，推理数据不做持久化存储 |
| 数据隔离 | 项目级数据严格隔离，不同租户数据不可交叉访问 |
| 数据不训练 | 用户推理数据不用于平台自身的模型训练 |

### 7.3 网络安全

| 措施 | 说明 |
|------|------|
| VPC Peering / PrivateLink | 企业客户通过私有网络连接，避免公网暴露 |
| IP 白名单 | 限制 API 访问来源 IP |
| DDoS 防护 | CloudFlare / AWS Shield 防御 |
| WAF | Web 应用防火墙，防御 OWASP Top 10 |

### 7.4 内容安全

| 措施 | 说明 |
|------|------|
| Safety Guardrails | 可插拔的输入/输出安全过滤（如 Llama Guard） |
| 内容审核 | 违规内容检测与拦截 |
| AUP | 可接受使用政策，明确禁止的使用场景 |
| 审计日志 | 所有管理操作和敏感 API 调用全记录 |

### 7.5 合规认证

| 认证 | 说明 |
|------|------|
| SOC 2 Type II | 安全、可用性、处理完整性的独立审计 |
| GDPR | 欧盟数据保护合规，支持 EU 数据驻留 |
| HIPAA BAA | 医疗行业数据处理合规 |
| DPA | 数据处理协议，明确数据控制者/处理者责任 |

---

## 8. 非功能性需求

### 8.1 性能要求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| TTFT (首 Token 延迟) | < 200ms (Fast) / < 500ms (Base) | 7B 模型基准 |
| TPS (生成速度) | > 80 tokens/s (Fast) / > 50 tokens/s (Base) | 7B 模型基准 |
| API Gateway 延迟 | < 10ms (P99) | 不含推理时间 |
| 并发请求数 | > 10,000 RPM | 平台整体 |

### 8.2 可用性要求

| 指标 | 目标值 |
|------|--------|
| 整体可用性 SLA | 99.9% (标准) / 99.95% (企业) |
| 单区域可用性 | 99.9% |
| 计划内维护窗口 | < 4 小时/月，提前 72 小时通知 |
| 故障恢复时间 (RTO) | < 15 分钟 |
| 数据恢复点 (RPO) | < 1 小时（元数据）/ < 24 小时（日志数据） |

### 8.3 可扩展性要求

| 维度 | 能力 |
|------|------|
| 水平扩展 | 所有无状态服务支持水平扩展 |
| GPU 节点扩展 | 通过 Cluster Autoscaler 动态增减 GPU 节点 |
| 多区域部署 | 支持 3+ 区域独立部署，区域间数据不默认同步 |
| 多租户 | 单集群支持 1000+ 组织 / 10000+ 项目 |

---

## 9. 实施路线图

### Phase 1: MVP — 核心推理能力

**目标**：搭建可用的基础推理服务，验证技术栈和产品方向

| 交付物 | 说明 |
|--------|------|
| 推理集群 (vLLM v0.18+) | 部署 5-10 个主流开源模型（DeepSeek-V4, Qwen3.5, GLM-5, Llama 4 等）；启用 FlashAttention-4、自动前缀缓存、连续批处理 |
| 双格式 API | Chat Completions (`/v1/chat/completions`) + Responses API (`/v1/responses`)，含 Items 模型、`previous_response_id` 多轮引用、Response Store |
| API Key 认证 | 基础 API Key 管理和验证 |
| 基础限流 | 固定窗口 RPM/TPM 限制 + Service Tier 机制（auto / default / flex） |
| Web Console | 模型列表、API Key 管理、基础 Playground |
| 迁移指南 | Switch Guide 文档与代码示例，支持 OpenAI 等平台零成本迁移 |
| 基础计费 | 按 Token 计量，余额扣减，Promo Code 充值 |
| 基础监控 | Prometheus + Grafana 集群监控 |

### Phase 2: 增强 — 专属端点与微调

**目标**：补全生产级核心功能，支撑中型客户需求

| 交付物 | 说明 |
|--------|------|
| 专属端点 | Template / Endpoint 完整生命周期管理 |
| 自动伸缩 | KEDA + Cluster Autoscaler 集成 |
| 推理优化 | 投机解码 (EAGLE-3 / N-gram GPU)、FP8/NVFP4 量化、KV Cache 压缩与 CPU 卸载 |
| LoRA 微调 | 训练任务管理、数据集验证、Checkpoint 管理 |
| Full Fine-tuning | 全参数训练流水线 |
| 批量推理 | Batch API 完整实现 |
| Playground 增强 | 模型对比 (Compare)、代码导出、Function Calling 测试、结构化输出测试 |
| Datasets API | 数据集 CRUD、分片上传、SQL 查询过滤、CSV/JSONL 导出 |
| 多区域部署 | 2 个区域上线 |

### Phase 3: 生态 — 可观测性与集成

**目标**：完善平台生态和运维能力

| 交付物 | 说明 |
|--------|------|
| Data Lab | 推理日志采集、Chat Completions 导入、SQL 查询、数据集管理 |
| 完整可观测性 | 延迟/吞吐/错误/伸缩全维度仪表盘 |
| Prometheus 导出 | 外部监控集成 API |
| 第三方集成 | 框架 (LangChain, LlamaIndex, CrewAI 等) + Coding Assistants (Cursor, Continue, Cline) + API Routers |
| 扩展模型类型 | Embedding + Rerank (`/v1/rerank`) + Text-to-Image |
| Prompt Preset | 管理与团队共享 |
| Cookbook | 官方开发者示例代码库 (GitHub 开源) |
| **CLI 命令行工具** | 官方 CLI 客户端（Go 单二进制），覆盖推理/微调/批量/端点/数据集全流程；Homebrew/npm/pip/Docker 多渠道分发；交互式对话 + 脚本化 CI/CD 双模式 |
| 动态限流 | 15 分钟滚动窗口自动伸缩机制 |
| PD 分离 | Prefill-Decode 分离部署 + NVIDIA Dynamo 编排 + KV-aware 路由 |
| MoE 专家并行 | 弹性 EP + DeepEP 通信优化，支持 DeepSeek-V4 等超大 MoE 模型高效推理 |

### Phase 4: 企业 — 安全合规与企业级

**目标**：满足企业客户的安全合规和管理需求

| 交付物 | 说明 |
|--------|------|
| SSO 集成 | SAML 2.0 / OIDC + 主流 IdP 适配 |
| 完整 RBAC | 组织 / 项目多级权限体系 + 自定义角色 |
| 零数据留存 | ZDR 模式实现 |
| 合规认证 | SOC 2 Type II 启动 / GDPR 合规 |
| 私有网络 | VPC Peering / PrivateLink |
| 企业 SLA | 99.95% SLA 保障 |
| 自定义模型 | 用户上传自有模型权重部署 (Custom Models API) |
| LoRA 合并工具 | LoRA 适配器权重合并到 base model 的脚本工具 |
| 企业计费 | 银行转账 / 企业费用中心 / 发票管理 |

### 持续演进方向

- AF (Attention-FFN) 分离部署 — 更细粒度异构推理，FFN 可使用国产 GPU
- Function Calling / Tool Use 增强与标准化
- 结构化输出 (JSON Schema) 深度优化
- RLHF / DPO 强化学习微调
- 语义缓存 — 基于向量嵌入的推理结果复用
- 多模态扩展（Audio / Video）
- 边缘推理节点
- 模型评测 Benchmark 平台
- 模型市场 (Model Marketplace)

---

## 10. 风险与应对

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| **GPU 供应紧张** | 无法及时扩容，影响服务质量 | 高 | 多云 GPU 供应商分散采购；预留长期合约；支持多种 GPU 型号 |
| **vLLM 版本兼容** | 新模型支持滞后 | 中 | 紧跟社区版本迭代；保留 SGLang 作为备选引擎 |
| **模型许可证变更** | 部分模型限制商用 | 中 | 建立模型许可证审查流程；优先支持 Apache 2.0 / MIT 许可模型 |
| **安全漏洞** | 数据泄露风险 | 中 | 定期安全审计；漏洞赏金计划；依赖自动扫描 (Dependabot) |
| **成本控制** | GPU 闲置率高导致亏损 | 中 | 智能调度最大化 GPU 利用率；批量推理填充空闲算力；Spot 实例混合使用 |
| **竞品价格战** | 利润率被压缩 | 高 | 差异化功能（企业级能力、微调闭环）；优化推理效率降低单位成本 |
| **客户数据合规** | 不满足行业监管要求 | 中 | 提前启动 SOC 2 / GDPR 认证；ZDR 模式；多区域数据驻留 |

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| TTFT | Time to First Token，首个 Token 延迟 |
| TPS | Tokens Per Second，每秒生成 Token 数 |
| RPM | Requests Per Minute，每分钟请求数 |
| TPM | Tokens Per Minute，每分钟 Token 数 |
| KV Cache | Key-Value Cache，注意力层缓存 |
| LoRA | Low-Rank Adaptation，低秩适配微调方法 |
| ZDR | Zero Data Retention，零数据留存 |
| SSE | Server-Sent Events，服务端推送事件流 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| DPA | Data Processing Agreement，数据处理协议 |
| HPA | Horizontal Pod Autoscaler，水平 Pod 自动伸缩 |
| KEDA | Kubernetes Event-Driven Autoscaling |
| OTel | OpenTelemetry，开源可观测性框架 |

### B. 参考资料

- [OpenAI API Reference](https://platform.openai.com/docs/api-reference)
- [vLLM Project](https://github.com/vllm-project/vllm)
- [SGLang Project](https://github.com/sgl-project/sglang)
- [NVIDIA GPU Operator](https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/)
- [Temporal Workflow Engine](https://temporal.io/)
- [KEDA Documentation](https://keda.sh/)
- [Volcano Scheduler](https://volcano.sh/)

---

*文档版本: v1.0 | 最后更新: 2026-04-03*
