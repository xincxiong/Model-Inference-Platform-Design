---
description: Git 推送策略 — 禁止未经用户确认的 git push
alwaysApply: true
---

# Git Push 必须用户确认

- 执行 `git commit` 后，**不得**自动执行 `git push`
- 必须先向用户展示提交摘要，**等待用户明确确认**后才能推送
- 同样适用于 `git push --force`、`git push --force-with-lease` 等所有推送操作
